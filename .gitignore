__pycache__/
*.pyc
.venv/

# Local config — holds GITHUB_TOKEN for private Repo Audit clones. Never commit.
.env

# Local clones dropped in for Repo Audit — keep the folder, ignore its contents
/audit/*
!/audit/.gitkeep

# Availability Dashboard SQLite store (created at runtime in the mounted volume)
/data/
